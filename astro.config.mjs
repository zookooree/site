// @ts-check
import { defineConfig } from "astro/config";

// Static output (Astro's default), no adapter, no client-side JavaScript.
// Cloudflare Pages serves the `dist/` directory as-is.
export default defineConfig({});
